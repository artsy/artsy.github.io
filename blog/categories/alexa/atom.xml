<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Alexa | Artsy Engineering]]></title>
  <link href="https://artsy.github.io/blog/categories/alexa/atom.xml" rel="self"/>
  <link href="https://artsy.github.io/"/>
  <updated>2024-03-13T03:23:08+00:00</updated>
  <id>https://artsy.github.io/</id>
  <author>
    <name><![CDATA[]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Bringing Artsy to Amazon Echo "Alexa"]]></title>
    <link href="https://artsy.github.io/blog/2016/11/30/bringing-artsy-to-amazon-echo-alexa/"/>
    <updated>2016-11-30T00:00:00+00:00</updated>
    <id>https://artsy.github.io/blog/2016/11/30/bringing-artsy-to-amazon-echo-alexa</id>
    <content type="html"><![CDATA[<p>tl;dr You can try Artsy on your Amazon Echo now, say “Alexa, enable Artsy” or see <a href="http://alexa.artsy.net">alexa.artsy.net</a> for more info.</p>

<p>With its powerful automatic speech recognizer, accurate natural language understanding and outstanding text-to-speech capabilities, the Amazon Echo, nicknamed “Alexa”, always impressed me. While not the first in its category and introduced in late 2014, Alexa was the first consumer device in my home to truly enable the conversation between human and machine. It was stationary, always listening for a wake word, and clearly outperformed all previous attempts when it came to the ability to receive commands from the other side of the apartment.</p>

<p>Alexa knows about the weather, but it doesn’t know much about art.</p>

<p>In this post I’ll dig a little inside the Alexa software platform and go over the technical details of bringing Artsy to the Echo, starting with a very simple “Ask Artsy about Norman Rockwell.”</p>

<iframe width="280" height="280" src="https://www.youtube.com/embed/zi3OubNiV9U" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""></iframe>

<p>Find the <a href="http://alexa.amazon.com/spa/index.html#skills/dp/B01MYLJO1N">Artsy skill in the Alexa app</a> and <a href="https://github.com/artsy/elderfield">the complete Skill code on Github</a>. And if you just want to learn to write a skill quickly, watch <a href="https://www.youtube.com/watch?v=pzM4jv7k7Rg">@dblock live-code a skill in this video</a>.</p>

<!-- more -->

<h3 id="how-does-alexa-work">How does Alexa work?</h3>

<p><em>Alexa</em> is the software platform used by Alexa-enabled devices such as the <em>Amazon Echo</em> or the <em>Echo Dot</em>. I used a $49.- Dot with headphones for most testing, and I have a bigger Echo at home (the main difference is the quality of the speaker). Because even a smaller Dot was sufficiently annoying to my coworkers, I wish it were battery powered, so I wouldn’t need to unplug it to go run tests in a conference room.</p>

<p>Alexa waits for a <em>wake word</em>, either “Alexa” or “Amazon”. It’s always on and has a built-in speech recognizer for the wake word only. After it hears the wake word, a blue light ring turns on and the device starts transmitting the user’s voice, called an <em>utterance</em> to the Alexa platform in the cloud. The light ring is the indicator of it “listening”. The cloud service will translate speech to text and run it through a natural language system to identify an <em>intent</em>, such as “ask Artsy about”. The intent is sent to a <em>skill</em>, which is a piece of software living in the cloud that can generate a <em>directive</em> to “speak” along with markup in <a href="https://www.w3.org/TR/speech-synthesis">SSML</a> format, transformed into voice and finally sent in wave format back to the device to be played back to the user.</p>

<h3 id="developer-prerequisites">Developer Prerequisites</h3>

<p>You need an <a href="https://developer.amazon.com">Amazon Apps and Services Developer account</a> and access to <a href="https://console.aws.amazon.com/lambda">AWS Lambda</a>. I’ve implemented the Artsy skill in node.js using <a href="https://github.com/matt-kruse/alexa-app">alexa-app</a>. You can find the <a href="https://github.com/artsy/elderfield">complete Skill code on Github</a>.</p>

<h3 id="designing-an-intent">Designing an Intent</h3>

<p>To get Alexa to listen, you must design an intent. Each intent is identified by a <em>name</em> and a set of <em>slots</em>. This is an order of magnitude more limited than what the Alexa system is capable of doing out of the box. It means that the platform <em>will not</em> transcribe any text for your application, chatbot-style. The intents have to be simple and clear and use English language words or predefined vocabularies. Furthermore, Alexa has a lot of trouble with names as it constantly attempts to recognize them as dictionary words and no support for “wildcards”.</p>

<p>I started implementing a fairly complicated “when is an artist born” intent, which turned to be too ambitious for Alexa to understand and ended up with a much simpler “ask artsy about an artist”. I called this “AboutIntent”, which takes an artist name as input (or slot).</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
   </span><span class="nl">"intents"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
         </span><span class="nl">"intent"</span><span class="p">:</span><span class="w"> </span><span class="s2">"AboutIntent"</span><span class="p">,</span><span class="w">
         </span><span class="nl">"slots"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
            </span><span class="p">{</span><span class="w">
               </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"VALUE"</span><span class="p">,</span><span class="w">
               </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"NAME"</span><span class="w">
            </span><span class="p">}</span><span class="w">
         </span><span class="p">]</span><span class="w">
      </span><span class="p">}</span><span class="w">
   </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>The only possible sample utterance of this intent is “about {VALUE}”, the “ask Artsy” portion is implied.</p>

<p>Since Alexa cannot understand artist names out of the box, I had to teach it with a custom, user-defined slot type added to the “Skill Interaction Model” with about a thousand most popular artist names on Artsy.</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/alexa-interaction-model.png" alt="Alexa interaction model" /></p>

<h3 id="implementing-a-skill">Implementing a Skill</h3>

<p>Intents are the API of a skill, otherwise known as an Alexa app. Using <a href="https://github.com/matt-kruse/alexa-app">alexa-app</a> makes implementing intents easy.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">var</span> <span class="nx">alexa</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">alexa-app</span><span class="dl">'</span><span class="p">);</span>
<span class="kd">var</span> <span class="nx">app</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">alexa</span><span class="p">.</span><span class="nx">app</span><span class="p">(</span><span class="dl">'</span><span class="s1">artsy</span><span class="dl">'</span><span class="p">);</span>

<span class="nx">app</span><span class="p">.</span><span class="nx">launch</span><span class="p">(</span><span class="kd">function</span><span class="p">(</span><span class="nx">req</span><span class="p">,</span> <span class="nx">res</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">res</span>
        <span class="c1">// welcome message</span>
        <span class="p">.</span><span class="nx">say</span><span class="p">(</span><span class="dl">"</span><span class="s2">Welcome to Artsy! Ask me about an artist.</span><span class="dl">"</span><span class="p">)</span>
        <span class="c1">// don't close the session, wait for user input (an artist name)</span>
        <span class="c1">// and provide a re-prompt in case the user says something meaningless</span>
        <span class="p">.</span><span class="nx">shouldEndSession</span><span class="p">(</span><span class="kc">false</span><span class="p">,</span> <span class="dl">"</span><span class="s2">Ask me about an artist. Say help if you need help or exit any time to exit.</span><span class="dl">"</span><span class="p">)</span>
        <span class="c1">// speak the response</span>
        <span class="p">.</span><span class="nx">send</span><span class="p">();</span>
<span class="p">});</span>

<span class="nx">app</span><span class="p">.</span><span class="nx">intent</span><span class="p">(</span><span class="dl">'</span><span class="s1">AboutIntent</span><span class="dl">'</span><span class="p">,</span> <span class="p">{</span>
        <span class="dl">"</span><span class="s2">slots</span><span class="dl">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="dl">"</span><span class="s2">VALUE</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">NAME</span><span class="dl">"</span>
        <span class="p">},</span>
        <span class="dl">"</span><span class="s2">utterances</span><span class="dl">"</span><span class="p">:</span> <span class="p">[</span>
            <span class="dl">"</span><span class="s2">about {-|VALUE}</span><span class="dl">"</span>
        <span class="p">]</span>
    <span class="p">},</span>
    <span class="kd">function</span><span class="p">(</span><span class="nx">req</span><span class="p">,</span> <span class="nx">res</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// intent implementation goes here</span>
    <span class="p">}</span>
<span class="p">});</span>
</code></pre></div></div>

<p>The skill expects a slot value as defined in the intent above.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">var</span> <span class="nx">value</span> <span class="o">=</span> <span class="nx">req</span><span class="p">.</span><span class="nx">slot</span><span class="p">(</span><span class="dl">'</span><span class="s1">VALUE</span><span class="dl">'</span><span class="p">);</span>

<span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="nx">value</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">res</span>
    <span class="p">.</span><span class="nx">say</span><span class="p">(</span><span class="dl">"</span><span class="s2">Sorry, I didn't get that artist name.</span><span class="dl">"</span><span class="p">)</span>
    <span class="c1">// don't close the session, wait for user input again (an artist name)</span>
    <span class="p">.</span><span class="nx">shouldEndSession</span><span class="p">(</span><span class="kc">false</span><span class="p">,</span> <span class="dl">"</span><span class="s2">Ask me about an artist. Say help if you need help or exit any time to exit.</span><span class="dl">"</span><span class="p">);</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
  <span class="c1">// asynchronously lookup the artist in the Artsy API, read their bio</span>
  <span class="c1">// tell alexa-app that we're performing an asynchronous operation by returning false</span>
  <span class="k">return</span> <span class="kc">false</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>We use the <a href="https://developers.artsy.net">Artsy API</a> to implement the actual skill. There’s not much more to it.</p>

<h3 id="skill-development-mode">Skill Development Mode</h3>

<p>I found it convenient to host the skill inside an <a href="https://github.com/matt-kruse/alexa-app-server">alexa-app-server</a> in development. It automatically loads skills from subdirectories with the following directory structure.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+--- server.js                  // the alexa-app-server host for development
+--- package.json               // dependencies of the host
+--- project.json               // lambda settings for deployment with apex
+----functions                  // all skills
     +--artsy                   // the artsy skill
        +--function.json        // describes the skill lambda function
        +--package.json         // dependencies of the skill
        +--index.js             // skill intent implementation
        +--schema.json          // exported skill intent schema
        +--utterances.txt       // exported skill uterrances
        +--node_modules         // modules from npm install
+--- node_modules               // modules from npm install
</code></pre></div></div>

<p>Note that the server exports the Express.js server for testing.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">var</span> <span class="nx">AlexaAppServer</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">alexa-app-server</span><span class="dl">'</span><span class="p">);</span>

<span class="nx">AlexaAppServer</span><span class="p">.</span><span class="nx">start</span><span class="p">({</span>
    <span class="na">port</span><span class="p">:</span> <span class="mi">8080</span><span class="p">,</span>
    <span class="na">app_dir</span><span class="p">:</span> <span class="dl">"</span><span class="s2">functions</span><span class="dl">"</span><span class="p">,</span>
    <span class="na">post</span><span class="p">:</span> <span class="kd">function</span><span class="p">(</span><span class="nx">server</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">module</span><span class="p">.</span><span class="nx">exports</span> <span class="o">=</span> <span class="nx">server</span><span class="p">.</span><span class="nx">express</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">});</span>
</code></pre></div></div>

<h3 id="skill-modes">Skill Modes</h3>

<p>The skill is mounted standalone in AWS lambda, under alexa-app-sever in development and otherwise will export schema and utterances. It decides what to do based on <code class="language-plaintext highlighter-rouge">process.env['ENV']</code>, loaded from a “.env” file. The committed version of this file sets the value to “development”.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{
    "ENV": "development"
}
</code></pre></div></div>

<p>The server loads <code class="language-plaintext highlighter-rouge">.env</code> from <code class="language-plaintext highlighter-rouge">server.js</code>.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">var</span> <span class="nx">config</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">./.env.json</span><span class="dl">'</span><span class="p">)</span>

<span class="k">for</span> <span class="p">(</span><span class="kd">var</span> <span class="nx">key</span> <span class="k">in</span> <span class="nx">config</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">[</span><span class="nx">key</span><span class="p">]</span> <span class="o">=</span> <span class="nx">config</span><span class="p">[</span><span class="nx">key</span><span class="p">]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Finally, the skill can do different things in different environments.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">[</span><span class="dl">'</span><span class="s1">ENV</span><span class="dl">'</span><span class="p">]</span> <span class="o">==</span> <span class="dl">'</span><span class="s1">lambda</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">exports</span><span class="p">.</span><span class="nx">handle</span> <span class="o">=</span> <span class="nx">app</span><span class="p">.</span><span class="nx">lambda</span><span class="p">();</span> <span class="c1">// AWS lambda</span>
<span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">[</span><span class="dl">'</span><span class="s1">ENV</span><span class="dl">'</span><span class="p">]</span> <span class="o">==</span> <span class="dl">'</span><span class="s1">development</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">module</span><span class="p">.</span><span class="nx">exports</span> <span class="o">=</span> <span class="nx">app</span><span class="p">;</span> <span class="c1">// development mode</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="kd">var</span> <span class="nx">fs</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">fs</span><span class="dl">'</span><span class="p">);</span>
    <span class="c1">// export schema and utterances</span>
    <span class="c1">// copy-paste the contents into the Interaction Model</span>
    <span class="nx">fs</span><span class="p">.</span><span class="nx">writeFileSync</span><span class="p">(</span><span class="dl">'</span><span class="s1">schema.json</span><span class="dl">'</span><span class="p">,</span> <span class="nx">app</span><span class="p">.</span><span class="nx">schema</span><span class="p">());</span>
    <span class="nx">fs</span><span class="p">.</span><span class="nx">writeFileSync</span><span class="p">(</span><span class="dl">'</span><span class="s1">utterances.txt</span><span class="dl">'</span><span class="p">,</span> <span class="nx">app</span><span class="p">.</span><span class="nx">utterances</span><span class="p">());</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="development-and-test">Development and Test</h3>

<p>Running the server locally with <code class="language-plaintext highlighter-rouge">node server.js</code> will create a simulator console on http://localhost:8080/alexa/artsy.</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/console.png" alt="lambda configuration" /></p>

<p>We will use the test console to produce test JSON. The about intent translates into the following.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"session"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"sessionId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"SessionId..."</span><span class="p">,</span><span class="w">
        </span><span class="nl">"application"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
            </span><span class="nl">"applicationId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"amzn1.ask.skill..."</span><span class="w">
        </span><span class="p">},</span><span class="w">
        </span><span class="nl">"attributes"</span><span class="p">:</span><span class="w"> </span><span class="p">{},</span><span class="w">
        </span><span class="nl">"user"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
            </span><span class="nl">"userId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"amzn1.ask.account..."</span><span class="w">
        </span><span class="p">},</span><span class="w">
        </span><span class="nl">"new"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="nl">"request"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"IntentRequest"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"requestId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"EdwRequestId..."</span><span class="p">,</span><span class="w">
        </span><span class="nl">"locale"</span><span class="p">:</span><span class="w"> </span><span class="s2">"en-US"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"timestamp"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2016-11-12T22:03:54Z"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"intent"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
            </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"AboutIntent"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"slots"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
                </span><span class="nl">"VALUE"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
                    </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"VALUE"</span><span class="p">,</span><span class="w">
                    </span><span class="nl">"value"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Norman Rockwell"</span><span class="w">
                </span><span class="p">}</span><span class="w">
            </span><span class="p">}</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>A mocha test uses the Alexa app server to make an HTTP request using the above intent data and expects well defined SSML output.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">chai</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">chai</span><span class="dl">'</span><span class="p">);</span>
<span class="nx">expect</span> <span class="o">=</span> <span class="nx">chai</span><span class="p">.</span><span class="nx">expect</span><span class="p">;</span>
<span class="nx">chai</span><span class="p">.</span><span class="nx">use</span><span class="p">(</span><span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">chai-string</span><span class="dl">'</span><span class="p">));</span>
<span class="nx">chai</span><span class="p">.</span><span class="nx">use</span><span class="p">(</span><span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">chai-http</span><span class="dl">'</span><span class="p">));</span>

<span class="kd">var</span> <span class="nx">server</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">../server</span><span class="dl">'</span><span class="p">);</span>

<span class="nx">describe</span><span class="p">(</span><span class="dl">'</span><span class="s1">artsy alexa</span><span class="dl">'</span><span class="p">,</span> <span class="kd">function</span><span class="p">()</span> <span class="p">{</span>
    <span class="nx">it</span><span class="p">(</span><span class="dl">'</span><span class="s1">tells me about Norman Rockwell</span><span class="dl">'</span><span class="p">,</span> <span class="kd">function</span><span class="p">(</span><span class="nx">done</span><span class="p">)</span> <span class="p">{</span>
        <span class="kd">var</span> <span class="nx">aboutIntentRequest</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">./AboutIntentRequest.json</span><span class="dl">'</span><span class="p">);</span>
        <span class="nx">chai</span><span class="p">.</span><span class="nx">request</span><span class="p">(</span><span class="nx">server</span><span class="p">)</span>
            <span class="p">.</span><span class="nx">post</span><span class="p">(</span><span class="dl">'</span><span class="s1">/alexa/artsy</span><span class="dl">'</span><span class="p">)</span>
            <span class="p">.</span><span class="nx">send</span><span class="p">(</span><span class="nx">aboutIntentRequest</span><span class="p">)</span>
            <span class="p">.</span><span class="nx">end</span><span class="p">(</span><span class="kd">function</span><span class="p">(</span><span class="nx">err</span><span class="p">,</span> <span class="nx">res</span><span class="p">)</span> <span class="p">{</span>
                <span class="nx">expect</span><span class="p">(</span><span class="nx">res</span><span class="p">.</span><span class="nx">status</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">equal</span><span class="p">(</span><span class="mi">200</span><span class="p">);</span>
                <span class="kd">var</span> <span class="nx">data</span> <span class="o">=</span> <span class="nx">JSON</span><span class="p">.</span><span class="nx">parse</span><span class="p">(</span><span class="nx">res</span><span class="p">.</span><span class="nx">text</span><span class="p">);</span>
                <span class="nx">expect</span><span class="p">(</span><span class="nx">data</span><span class="p">.</span><span class="nx">response</span><span class="p">.</span><span class="nx">outputSpeech</span><span class="p">.</span><span class="nx">type</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">equal</span><span class="p">(</span><span class="dl">'</span><span class="s1">SSML</span><span class="dl">'</span><span class="p">)</span>
                <span class="kd">var</span> <span class="nx">ssml</span> <span class="o">=</span> <span class="nx">data</span><span class="p">.</span><span class="nx">response</span><span class="p">.</span><span class="nx">outputSpeech</span><span class="p">.</span><span class="nx">ssml</span><span class="p">;</span>
              <span class="nx">expect</span><span class="p">(</span><span class="nx">ssml</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">startWith</span><span class="p">(</span><span class="dl">'</span><span class="s1">&lt;speak&gt;American artist Norman Rockwell </span><span class="dl">'</span><span class="p">);</span>
              <span class="nx">done</span><span class="p">();</span>
            <span class="p">});</span>
    <span class="p">});</span>
<span class="p">});</span>
</code></pre></div></div>

<h3 id="lambda-deployment">Lambda Deployment</h3>

<p>The production version of the Alexa skill is a Lambda function.</p>

<p>I created an “alexa-artsy” function with a new IAM role, “alexa-artsy” in AWS Lambda and copy-pasted the role URN into “project.json”. This is a file used by <a href="https://github.com/apex/apex">Apex</a>, a Lambda deployment tool (<code class="language-plaintext highlighter-rouge">curl https://raw.githubusercontent.com/apex/apex/master/install.sh | sh</code>) along with awscli (<code class="language-plaintext highlighter-rouge">brew install awscli</code>). I had to configure access to AWS the first time, too (<code class="language-plaintext highlighter-rouge">aws configure</code>).</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/lambda-configuration.png" alt="lambda configuration" /></p>

<p>In order to connect the lambda function with an Alexa skill, I added an “Alexa Skills Kit” trigger. Without this you get an obscure “Please make sure that Alexa Skills Kit is selected for the event source type of arn:…” error.</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/lambda-triggers.png" alt="lambda triggers" /></p>

<p>I configured the “Service Endpoint” in the Alexa Skill configuration to point to my Lambda function.</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/alexa-skill-configuration.png" alt="Alexa skill configuration" /></p>

<p>I deployed the lambda function with <code class="language-plaintext highlighter-rouge">apex deploy</code> and test the skill with <code class="language-plaintext highlighter-rouge">apex invoke</code> or from the Alexa test UI.</p>

<p>Each time we do a <code class="language-plaintext highlighter-rouge">apex deploy</code> it will create a new version of our function and will update <code class="language-plaintext highlighter-rouge">$LATEST</code> <a href="http://docs.aws.amazon.com/lambda/latest/dg/versioning-aliases.html">Lambda alias</a>. One way to handle versioning is to create a <code class="language-plaintext highlighter-rouge">$PROD</code> Lambda alias and point to <code class="language-plaintext highlighter-rouge">$PROD</code> when setting function in “Service Endpoint” step and each time we want to upate prod we need to update <code class="language-plaintext highlighter-rouge">$PROD</code> alias to point to latest version.</p>

<p>Also note that after each deploy with apex we need to set environment variables for the newly created version.</p>

<p>Logs didn’t appear in AWS Cloudwatch with the execution policy created by default, I had to give the IAM “alexa-artsy” role more access to “arn:aws:logs:<em>:</em>:*” via an additional inline policy.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"logs:CreateLogGroup"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"logs:CreateLogStream"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"logs:PutLogEvents"</span><span class="w">
      </span><span class="p">],</span><span class="w">
      </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:logs:*:*:*"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="testing-with-echoism">Testing with Echoism</h3>

<p>You can use <a href="https://echosim.io">echosim.io</a> to test your skill. It’s essentially identical to an Echo.</p>

<h3 id="testing-with-echo">Testing with Echo</h3>

<p>I got an Echo Dot. Test skills appear automatically in the Alexa configuration attached to my account!</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/alexa-skills.png" alt="alexa skills" /></p>

<p>I just try to <a href="https://www.youtube.com/watch?v=FYVOAU35Sio">talk to it</a>.</p>

<h3 id="certification-process">Certification Process</h3>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/certified-email.png" alt="certified email" /></p>

<p>This took several weeks of back-and-forth. I should have gone through the <a href="https://developer.amazon.com/public/solutions/alexa/alexa-skills-kit/docs/alexa-skills-kit-voice-interface-and-user-experience-testing">Alexa Skills Kit Voice Interface and User Experience Testing for Custom Skills</a> more thoroughly, but even after reading it a few times it wasn’t obvious what the reviewers wanted.</p>

<p><img src="/images/2016-11-30-bringing-artsy-to-amazon-echo-alexa/certified-dashboard.png" alt="certified dashboard" /></p>

<p>Keep in mind that you need to go through certification process only if you are changing descriptions and details of the skill. If you updated the lambda function without modifying <code class="language-plaintext highlighter-rouge">utterances</code> or <code class="language-plaintext highlighter-rouge">schema</code> you don’t have to go through certification process.</p>

<p>Here’s a list of my mistakes.</p>

<h4 id="sample-phrases">Sample Phrases</h4>

<p>The <em>Example Phrases</em> in the <em>Publishing Information</em> section of the certification UI must be present in your sample utterances. My three examples are <em>“Alexa, ask Artsy about Artsy”</em>, <em>“Alexa, ask Artsy about Andy Warhol”</em> and <em>“Alexa, ask Artsy about Norman Rockwell”</em>.</p>

<p>The sample utterances are in the <em>Interaction Model</em> section and must not include the wake word or any relevant launch phrasing. In the Artsy app I only have a single utterance with <em>“AboutIntent about {VALUE}”</em>, but lots of entries for <em>VALUE</em> in the custom slot type. I forgot to include <em>artsy</em> and <em>Norman Rockwell</em> in those.</p>

<p>The skill got rejected mutliple times with <em>about artsy</em> not working, so I eventually gave up on supporting that. Alexa would recognize it as <em>artsee</em>, <em>artzi</em> and many other variations. I suspect testers pronounce it <em>artsay</em> or <em>artsai</em> and never get the correct response.</p>

<h4 id="the-opening-line">The Opening Line</h4>

<p>The welcome prompt is required, and must describe what users can ask of the skill and the session must remain open for a user response. This means that in order to get your app certified you must build some kind of basic conversational ability. My first cut simply said <em>“Welcome to Artsy”</em>, which wasn’t good enough. I added a prompt, <em>“Ask me about an artist.”</em> and included <code class="language-plaintext highlighter-rouge">shouldEndSession(false)</code> in the code.</p>

<h4 id="stop-and-cancel-intents">Stop and Cancel Intents</h4>

<p>The skill must exit appropriately when users say <em>“stop”</em> or <em>“cancel”</em>. This means you must build at the very least an <em>AMAZON.StopIntent</em>, <em>AMAZON.CancelIntent</em>. In my first iteration a user would say <em>“Alexa open Artsy”</em>, then <em>“stop”</em> and would get <em>“Sorry, I didn’t get that artist name.”</em>.</p>

<h4 id="help-intent">Help Intent</h4>

<p>The skill must also implement <em>AMAZON.HelpIntent</em> as a question. In my first iteration a user would say <em>“Alexa open Artsy”</em>, then <em>“help”</em> and would get <em>“Sorry, I didn’t get that artist name.”</em>. Then help would say something like <em>“You can ask me about Artsy. Say cancel or stop at any time.”</em> The certification process requires a prompt, eg. <em>“What artist would you like to hear about?”</em> and the session must remain open via <code class="language-plaintext highlighter-rouge">shouldEndSession(false)</code>.</p>

<h3 id="skill-and-code">Skill and Code</h3>

<p>Find the <a href="http://alexa.amazon.com/spa/index.html#skills/dp/B01MYLJO1N">Artsy skill in the Alexa app</a>.</p>

<p>Find the <a href="https://github.com/artsy/elderfield">complete Skill code on Github</a>.</p>
]]></content>
  </entry>
  
</feed>
